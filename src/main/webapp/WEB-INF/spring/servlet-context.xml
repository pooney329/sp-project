<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/mvc
                           http://www.springframework.org/schema/mvc/spring-mvc.xsd
                           http://www.springframework.org/schema/context
                           http://www.springframework.org/schema/context/spring-context.xsd">

    <!--
        자식 컨텍스트 (Servlet WebApplicationContext)
        - DispatcherServlet 이 로드
        - Controller, ControllerAdvice 빈 담당
        - 부모 컨텍스트(root-context.xml)의 Service/Mapper 빈 참조 가능
    -->

    <!-- @Controller, @ControllerAdvice 만 스캔 -->
    <context:component-scan base-package="com.test" use-default-filters="false">
        <context:include-filter type="annotation"
            expression="org.springframework.stereotype.Controller"/>
        <context:include-filter type="annotation"
            expression="org.springframework.web.bind.annotation.ControllerAdvice"/>
    </context:component-scan>

    <!-- 어노테이션 기반 URL 매핑 활성화 (@RequestMapping 등) -->
    <mvc:annotation-driven/>

    <!-- 컨트롤러가 처리 못한 요청은 Tomcat 기본 서블릿으로 위임 (정적 파일 등) -->
    <mvc:default-servlet-handler/>

    <!-- 정적 리소스 처리 (/static/ 하위만) -->
    <mvc:resources mapping="/static/**" location="/static/"/>

    <!-- JSP 뷰 리졸버 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/view/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- JWT 인터셉터: 모든 요청에서 jwt 쿠키를 파싱해 loginMember request attribute 설정 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/**"/>
            <bean class="com.test.config.JwtInterceptor">
                <property name="jwtUtil" ref="jwtUtil"/>
            </bean>
        </mvc:interceptor>
    </mvc:interceptors>

</beans>
